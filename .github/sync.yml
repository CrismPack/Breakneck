CrismPack/Wiki:
  # Publish the presentation-free release data; the wiki renders the pages.
  # Directory sync is MC-version agnostic, so migrations never touch this.
  - source: Changelogs/data/
    dest: docs/breakneck/data/
